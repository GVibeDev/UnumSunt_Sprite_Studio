# Migration R5c3b → R5c3c

R5c3c fixes the WanGP settings contract only. Existing runtime binaries and model checkpoints are preserved.

The managed video bridge now uses the bundled official-style Animate settings template instead of any stale generic settings file previously stored in `local_wangp.json`.
